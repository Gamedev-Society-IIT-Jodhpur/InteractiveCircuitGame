using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Drag : MonoBehaviour
{
    /*bool mouseOver = false;
    [SerializeField] GameObject manager;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        if (Input.GetMouseButton(0) && mouseOver && !manager.GetComponent<manager>().isDraggin)
        {
            manager.GetComponent<manager>().isDraggin = true;
            Vector3 mousePosition = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            gameObject.transform.position = new Vector3(mousePosition.x,mousePosition.y,0);
        }
        else
        {
            manager.GetComponent<manager>().isDraggin= false;  
        }
    }

    void OnMouseExit()
    {
        if (!manager.GetComponent<manager>().isDraggin)
        {
            mouseOver = false;
        }
    }

    void OnMouseEnter()
    {
        if (!manager.GetComponent<manager>().isDraggin)
        {
        }
            mouseOver = true;
    }*/
}
